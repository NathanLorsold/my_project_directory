{% extends 'base.html.twig' %}
{% block title %}Accueil - Village Green
{% endblock %}
{%
block body %}

	<div class="container text-center">
		
		<div class="row my-5">
			<h3>Catalogue</h3>
			{% for item in produits %}
				{# Mettre les produits en card #}
			{% else %}
				<div>
					<p>Aucun enregistrement trouvé!</p>
				</div>
			{% endfor %}
		</div>
		
		<br>
		
		<div class="row my-5">
			<h3>Les mieux notés</h3>
			{% for item in produits %}
				{# Mettre les mieux notés #}
			{% else %}
				<div>
					<p>Aucun enregistrement trouvé!</p>
				</div>
			{% endfor %}
		</div>
	</div>
</div>

<div class="row p-5">
	<h3>Nos produits</h3>
	{% for item in produits %}
		{# Afficher les produits #}
	{% else %}
		<div>
			<p>Aucun enregistrement trouvé!</p>
		</div>
	{% endfor %}
</div>{% endblock %}
